// Planned unit coverage:
// - context service returns empty snapshot when ShareGameContext is off
// - friendship data is omitted unless enabled
// - installed mod list is omitted unless enabled
// - multiplayer player data is omitted by default
