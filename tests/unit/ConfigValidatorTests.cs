// Planned unit coverage:
// - null/missing config normalizes to safe defaults
// - outdated schema migrates to current schema
// - timeout/token/history/UI scale values clamp to safe defaults
// - provider URLs repair when blank
