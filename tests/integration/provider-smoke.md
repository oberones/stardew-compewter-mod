# Provider Smoke Scenarios

- [ ] Disabled provider returns setup guidance.
- [ ] Ollama local provider returns an answer when server/model are available.
- [ ] Unavailable Ollama returns friendly error.
- [ ] Custom OpenAI-compatible endpoint parses `choices[0].message.content`.
